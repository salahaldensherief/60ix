/// An enum representing the types of files that can be selected.
enum FileTypeSelect {
  /// To select a file from the device's camera.svg.
  camera,

  /// To select an image file.
  image,

  /// To select a video file.
  video,

  /// To select an image or video file.
  media,

  /// To select a document file.
  document,
}
