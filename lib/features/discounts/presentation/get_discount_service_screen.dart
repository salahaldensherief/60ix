import 'package:flutter/material.dart';

class GetDiscountServiceScreen extends StatelessWidget {
  const GetDiscountServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(),
    );
  }
}
